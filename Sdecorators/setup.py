from setuptools import find_packages, setup

setup(
    name='Sdecorators',
    version='1.0.0',
    author="Swarfte",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
    description="python Generic Decorator Sdecorators"
)
